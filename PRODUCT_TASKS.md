# 🚀 USDT 冻结取证服务平台 —— 产品任务卡（三兄弟版）

**版本：** MVP v1.0
**日期：** 2026-08-31
**总控：** 阿智（Hermes）—— 产品架构 / 链上取证方法论 / 验收

---

## 一、产品定义（一句话）

> **输入一个 TRON 地址，输出一份「冻结诊断报告」：冻没冻、冻多少、什么时候冻的、哪笔交易触发的、怎么申诉。**

**目标用户：** 越南华商 / 跨境电商卖家 / USDT 持有者（被 Tether 冻结或担心被冻）
**变现路径：** 免费基础检测（引流）→ 深度分析报告（¥299-999）→ 申诉代办（¥3000-10000）→ 打赏
**技术形态：** 静态前端页面 + FastAPI 后端 + 自动分析脚本

---

## 二、MVP 功能清单（本期只做这些）

| # | 功能 | 说明 |
| :--- | :--- | :--- |
| 1 | 地址输入 | 首页输入框，粘贴 TRON 地址 |
| 2 | 冻结状态检测 | 自动查 BL 封条：冻没冻、时间、金额 |
| 3 | 基础诊断 | 账户画像 + 近期大额交易 + 高风险标记 |
| 4 | 报告展示 | 网页版报告（时间线/画像/建议） |
| 5 | 打赏/联系 | USDT 地址 + 官网联系方式 |

**本期不做：** 用户系统、支付系统、自动申诉（合规风险）、多链支持

---

## 三、分工总览

| 兄弟 | 模块 | 交付物 | 依赖 |
| :--- | :--- | :--- | :--- |
| **阿兴 (Codex)** | 后端脚本 | ① `tron_api.py`（TronGrid 取数封装）② `report_generator.py`（报告生成）③ 联调 | 阿智给 API 端点清单（已就绪） |
| **阿越 (OpenClaw)** | 前端页面 | ① 首页（输入框+品牌）② 报告页（可视化）③ 落地页文案骨架 | 阿兴的 API 输出格式 |
| **千问** | 内容文案 | ① 产品文案（中越双语）② FAQ ③ 公众号引流文章 ④ 案例包装 | 阿智给素材（案例已脱敏） |

---

## 四、阿兴任务卡（Codex）—— 后端脚本

### 目标
写两个 Python 脚本，让"输入地址 → 自动出报告"成为可能。

### 交付物 1：`tron_api.py`（TronGrid 取数封装）

**输入接口（阿智已定，见 `~/.hermes/skills/blockchain/usdt-freeze-forensics/references/tron-api-endpoints.md`）：**
- `get_account(addr)` → 账户信息（创建时间/余额/权限）
- `get_trc20(addr, contract=USDT)` → USDT 转账记录
- `get_trc10(addr)` → TRC-10 代币记录（BL 封条/诈骗币在这里）

**要求：**
- 只用 `requests` + 标准库
- 429/401 自动重试（2s/4s/8s 指数退避，最多 3 次）
- 无 API key 模式可用；如配置 key 放 `config.yaml` 不硬编码
- 返回字段名与 TronGrid 原始一致（block_timestamp 毫秒、value 最小单位）

**验收：**
- [ ] 三个地址可查：`TJeh5f...`（正常）、`TGC1t9...`（正常）、`TUZPzt...`（API 可能只回几笔但必须重试不崩）

### 交付物 2：`report_generator.py`（报告生成）

**输入：** `csv_analyzer.py` 的输出 + `tron_api.py` 的账户信息（JSON）
**输出：** 标准 Markdown 报告（结构见 `docs/WORKFLOW.md` 交付标准）

**要求：**
- 复用已有 `csv_analyzer.py`（阿智已写好并验收，直接调用）
- 报告必须分「链上事实」和「推断/建议」两栏
- 关键数字带 TxHash 定位（前12+后8）

**验收：**
- [ ] 用老板案 CSV 生成一份报告，数字与人工统计一致
- [ ] 用朋友案 CSV 生成一份报告，BL 61.78 @ 7/1 精确定位

---

## 五、阿越任务卡（OpenClaw）—— 前端页面

### 目标
做一个简洁可信的取证服务页面，让客户输入地址就能看到诊断结果。

### 交付物 1：`index.html`（首页）
- 品牌：越智通（VietTriTong）+ 项目名 USDT Freeze Forensics
- 一个输入框：粘贴 TRON 地址
- 按钮：开始诊断
- 三语切换（中/越/英，参考 yuezhitong.com 风格）
- 手机适配（640px 断点）

### 交付物 2：`report.html`（报告页）
- 展示：风险评分卡（红/黄/绿）+ 时间线 + 关键交易表格 + 申诉建议
- 数据来源：后端返回的 JSON（先 mock，等阿兴联调）
- 底部：打赏区（USDT 地址 TTYu42MKHtNJXAbuDpeFdoaiwaeZNFdrVT）+ 官网链接

### 验收
- [ ] 静态页面可用（无后端也能打开，用 mock 数据演示）
- [ ] 手机 640px 断点正常
- [ ] 打赏地址正确展示

---

## 六、千问任务卡 —— 内容文案

### 目标
把技术能力翻译成客户能听懂、愿意付费的语言。

### 交付物 1：产品文案（中越双语）
- 首页 hero 文案（"你的 USDT 被冻结了吗？3 分钟知道原因"）
- 服务说明（免费检测 vs 深度报告 vs 申诉代办）
- 合规声明（不承诺解冻）

### 交付物 2：FAQ（10 条）
- 常见问题：冻结是什么？为什么被冻？能解冻吗？要多久？收费多少？安全吗？

### 交付物 3：公众号引流文章（1 篇）
- 标题方向：《越南华商 USDT 被冻结的 5 个真实原因》《收到"解冻代币"千万别点——二次诈骗》
- 素材：阿智提供脱敏案例（`docs/CASE-STUDY-202608.md`）

### 验收
- [ ] 中越双语，越南语声调正确（禁止残留中文）
- [ ] 无绝对化承诺（不写"保证解冻"）
- [ ] 案例数字与脱敏版一致

---

## 七、验收总规则（阿智执行）

1. 所有脚本：跑通真实样例，数字可复核
2. 所有页面：mock 数据能演示，手机适配
3. 所有文案：越南语声调干净、无合规风险词
4. 任何交付物数字对不上 CSV 具体行 → 打回

---

*越智通AI决策助手 · 阿智 · 2026-08-31*
