# 🎯 阿兴任务卡：usdt-forensics 埋点补齐 + 标签库单一事实来源

**来自：** 阿智（越智通AI决策助手）
**日期：** 2026-09-02
**优先级：** P0（埋点 5 文件是上线阻塞项）→ P1（标签库重构）

---

## 一、产品背景（2026-09-02 最新状态，别用旧认知）

USDT 冻结检测 `https://yuezhitong.com/usdt-check/` 已上线，关键升级：
- **冻结时间动态索引（本次新上）**：后端 `/opt/usdt-forensics/freeze_index.db`（SQLite，9154 条 AddedBlackList/RemovedBlackList 链上事件，sync_freeze_index.py 每日增量 cron）。`get_frozen_at()` 对**任意冻结地址**返回确切冻结时间。
- API：`/api/report?address=&lang=zh|vi|en` → JSON 含 `score`（顶层 int）、`frozen`（顶层 bool）、`timeline[]`、`scoreBreakdown[]`、`actionPlan[]`
- 后端 systemd `usdt-forensics`（uvicorn 127.0.0.1:8902）；nginx 精确匹配转发（`location = /api/report`、`= /api/deep-report` → 8902；`location /api/` 前缀 → 8008 是**其他服务**，勿混）
- 产品路径：广州 ECS `8.163.22.32`，前端 `/home/admin/yuezhi_tong/usdt-check/`，后端 `/opt/usdt-forensics/`

## 二、P0 任务：补齐埋点缺失文件（你 Windows 本机有）

你 8 月交付 `collector.py`（阿智已验 **49/49 PASS**）+ `collect.js`（已验 **16/17**，唯一 FAIL 是测试脚本断言笔误，非业务）——但**部署必需 5 文件未传输**：

### 缺失文件清单（在你本机 `C:\Users\Administrator\...\outputs\usdt-analytics\`）
| 文件 | 用途 | 阿智验收点 |
|------|------|------|
| `http_logic.py` | 端点逻辑（FastAPI + 测试共用，不依赖 uvicorn） | 导入无副作用；函数可单测 |
| `router_fastapi.py` | 挂到 8902：`POST /api/collect` + `GET /api/stats?token=` | 与 collector.py 的 `UA_*` env 约定一致 |
| `gen_events.py` | 生成测试事件（本地跑通用） | 可独立运行生成 events-*.jsonl |
| `verify.py` | 隐私断言：**地址原文不落盘**（sha256 前 8）/ IP HMAC 日桶 / 阈值 60/30 / 限流 | 断言全绿 + 输出可指到代码行 |
| `verify_collect.js` | collect.js 测试（**修正断言笔误版**） | 17/17 全绿 |

### 环境约定（collector.py 已验，5 文件必须一致）
- 事件文件 `events-YYYY-MM-DD.jsonl` 落 `/var/lib/usdt-forensics/analytics/`
- env：`UA_DATA_DIR` / `UA_SECRET` / `UA_STATS_TOKEN` / `UA_TRUST_PROXY`
- 地址 → sha256 前 8 指纹；IP/UA → 当日 HMAC 桶（**原文一律不落盘**）
- 限流 `/api/collect` 建议 30r/m（nginx `limit_req`）

### 交付方式
- **飞书群文件**（单文件逐个发，文件名带日期，如 `http_logic_20260902.py`），不要 base64 长文
- 阿智收到后 5 文件先过 verify.py/collector.py 回归再部署

## 三、P1 任务：标签库单一事实来源重构（你之前提的工程隐患，现在落地）

现状问题：地址标签三处分散——① `api/api_server.py` 的 `ADDRESS_TAGS` 常量 + `FROZEN_AT` 常量（硬编码）② 活库 `~/.hermes/skills/blockchain/usdt-freeze-forensics/references/blacklist-db.csv` ③ `sync_freeze_index.py` 产物（freeze_index.db，只存 hex 时间不存标签）。

### 要求
把**产品运行时标签**抽成单一数据文件 `api/data/address-tags.csv`（列：`address,tag,evidence,evidence_level`），`api_server.py` 启动加载：
- 保留现 `ADDRESS_TAGS` 全部 10 条 + `FROZEN_AT` 2 条（evidence 注明 AddedBlackList tx/时间）
- 加载失败回退内置常量（不崩）；标签查询优先 CSV
- **不改**：`scripts/sync_freeze_index.py`（只存 hex+时间，别让它兼职标签）
- 命名规范：`address-tags.csv`（小写连字符，与仓库文件名风格一致）

### 验收标准（阿智跑完核对）
- [ ] `api/data/address-tags.csv` 存在，列完整
- [ ] api_server 导入 + `address_tag("TJeh5f...")` == "已冻结·OTC资金枢纽"、`get_frozen_at("TJeh5f...")` == 2026-08-08 21:20:36 UTC
- [ ] 删掉 CSV 一条 → 重启后该地址标签消失（证明真的从 CSV 读，不是硬编码兜底）
- [ ] `python3 -m py_compile api_server.py` 通过

## 四、P2 任务（可选，做完更好）：固化 tests/

把这 3 轮 ad-hoc 验证固化成仓库 `tests/`：
- `tests/test_freeze_index.py`：get_frozen_at（老板/朋友/未知）/ _base58_to_hex20 / sync 产物断言
- `tests/test_i18n_keys.py`：三语键一致 + vi/en 无中文标点
- `tests/README.md`：怎么跑（`python3 -m pytest` 或裸脚本）

阿智当前验证是 /tmp hermes-verify-* 临时脚本，固化后 CI 可复用。

---

## ⚠️ 阿智验收铁律（对你老规矩）
1. **交付 = 文件在磁盘 + 数字/行为可指**（自报"完成/已贴"不算数——先查文件系统再报）
2. 任何 URL/数据禁止编造，必须可指到链上事件/CSV 行
3. 5 文件补齐后不要自己部署线上（阿智统一部署+重启+nginx）
4. 有疑问先飞书群问，不要自作主张改产品逻辑

---

**阿智收到后的动作：** 5 文件 → 本机回归（collector 49/49 + verify 全绿 + collect.js 17/17）→ 服务器部署（analytics 目录 + env + systemd + nginx limit_req + 三条 curl 验收）→ GitHub 同步。
